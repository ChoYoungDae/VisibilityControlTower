# Visibility Control Tower PRD

- 작성일: 2026-08-03 · 최종 개정: 2026-08-10
- 버전: v2.5 — §6 명칭/톤: 화면 상단 대시보드 명칭을 "Proactive Center"로
  변경하고 근거 추가(Full HD 화면 재구성 세션, `visibility_control_tower_
  fhd.html` 한정 — `visibility_control_tower_mockup.html`은 유지).
- 버전: v2.4 — §9.8(MVP 이후 발전 방향, 방향성 라벨만 있던 로드맵) 삭제.
  유일하게 요구사항성이 있던 문장("발주 실행은 범위 밖")은 §12로 이동.
  §10 vessel-tracker 한계에 SeaVantage 계정 whitelist 미등록 사실 반영
  (경위는 [`PROGRESS.md`](PROGRESS.md)).
- 근거 문서: [`VisibilityControlTower_기획.md`](VisibilityControlTower_기획.md), [`PROGRESS.md`](PROGRESS.md)
- 상태: 화물/업무/비용/재고 탭 모두 `visibility_control_tower_mockup.html`에
  반영, 사용자 리뷰 진행 중.

### 이 문서를 읽는 법

기능 요구사항에는 확정 수준을 나타내는 성숙도 태그를 붙인다.

| 태그 | 의미 | 판별 기준 |
|---|---|---|
| `[검증]` | 실데이터로 확인함 | 구체적 수치·건수가 있음 |
| `[설계]` | 사양 확정, 필요 데이터 확보 확인됨, 미구현 | 데이터 소스가 실재하고 접근 가능 |
| `[가정]` | 사양은 있으나 데이터 확보 여부 미확인 | "데이터가 있다는 전제로" |
| `[구상]` | 방향성만 있음 | 화면·계산식·데이터 어느 것도 미정의 |

본문은 **현재 사양만** 서술한다. 어떤 결정이 언제 왜 바뀌었는지의 이력은
`PROGRESS.md`가 담당한다. 개념마다 정본 위치는 한 곳이며, 다른 곳에서는
`(→ §X)`로만 참조한다.

---

## 1. 개요

본 서비스는 고객의 국제운송·통관 화물 정보와 재고 정보를 결합해 보여주는
Visibility 서비스다. 재고는 화물 정보에서 파생되는 것이 아니라, 서로 다른
소스(Transportation·W&D 등)의 정보를 Item 단위로 결합한 결과다(→ §9.4
재고 계산식). 현재 고객이 로그인 직후 자신의 전체 화물 상황과 그것이 재고에
미치는 영향을 한눈에 파악할 통합 진입점이 없거나 파편화되어 있다.

이 문서는 그 진입점을 "대시보드"가 아니라 **Visibility Control Tower**로
정의하고, 제품 요구사항을 정리한다.

## 2. 문제 정의

- 고객은 화물 위치를 전체 구간에 걸쳐 실시간으로 파악하기 어렵고, 파악한다
  해도 정확한 ETA를 예측하기는 아직 쉽지 않다. 트래킹 정보 자체에 오류가
  섞이기 쉬워, 서로 다른 소스 간 정보가 상충할 때 어느 쪽을 신뢰하거나
  어떻게 보정할지 판단이 필요하다.
- 업무(부킹/서류/Cut-off) 진행 상황과 비용(D&D 등) 리스크도 화물 상태와
  분리되어 있어, 담당자가 여러 화면·이메일·엑셀을 오가며 직접 종합해야 한다.
- 재고 계획의 정확도는 결국 입고 시점 정보의 정확도에 종속된다. 그런데 그
  시점을 확정하려면 선사가 공지하는 milestone과 실제 AIS 위치, 내부
  오퍼레이션 기록이 서로 상충할 때 어느 쪽을 더 신뢰할지 판단해야 하고,
  이 판단은 포워딩 실무를 아는 담당자만 할 수 있다. W&D나 ERP가 기능을
  아무리 추가해도 이 판단 자체를 대신할 수는 없다. 즉 이 문제는 W&D의
  기능 부재가 아니라 **구조적으로 포워딩 쪽에서 풀어야 하는 문제**다
  (→ §7).

## 3. 컨셉

> 가시성은 화물 상태에 국한하지 않는다. 그 화물이 만들어내는 업무(부킹·
> 서류·통관)와 비용(D&D 등) 리스크까지 함께 봐야 완결된 그림이 된다. 또한
> 물류 현장은 컨테이너·B/L 같은 운송 단위로 움직이지만, 고객이 실제로
> 궁금한 것은 결국 Item 단위다. 이 서비스는 운송 단위 가시성을 Item 단위
> 가시성으로 변환해, Transportation과 W&D 재고를 하나로 잇는
> 관제탑(Control Tower)이다.

## 4. 대상 사용자

- 본 서비스를 이용하는 화주(고객사) 담당자, 로그인 계정 기준 단일 유형 고객군.
- 화물량·업종별 세분화된 페르소나나 역할별(마케팅/물류담당 등) 화면 분기는
  현재 범위에서 다루지 않는다 — 모든 사용자가 같은 화면 전체(Transportation+
  Inventory)에 접근한다.
- 참고로 도메인 실무상 화물/업무/비용은 물류·수출입 운영 담당자에게, 재고
  (Projected Inventory/Shortage)는 Demand Planner·자재·구매 계획 담당자에게
  더 직접적으로 도움이 될 것으로 예상된다. 이는 화면 분기 근거가 아니라
  콘텐츠 우선순위·설명 톤을 잡을 때 참고하는 설명일 뿐이다.

## 5. 백본 구조 (Backbone)

```
부킹 → 선적 준비(CI/PL 확보) → 선적 → 트래킹(해상/항공) → 하역 → Inland Routing → Final Destination
                                                                          ↓ Inbound(+)
                                                                W&D Inventory(On-hand)
                                                                          ↑ Outbound(−, 고객 시스템에서 별도 발생)
```

- **Item 단위 트래킹의 출발점은 PO가 아니라 CI/PL 확보 시점이다.** PO는
  이상적인 소스지만, 포워더가 화주로부터 PO를 직접 받는 사례는 현실적으로
  드물다 — PO는 화주-공급업체 간 문서라 포워더가 당사자가 아니고, 단가·
  결제조건 등 상업 기밀이 담겨 있어 화주가 제3자에 공유하길 꺼린다. 반면
  CI/PL은 수출통관에 필수라 포워더가 정상적인 업무 흐름 안에서 이미 받는
  문서다. 이 판단이 §8.2 업무 탭 리스트 구조와 §9.1 도메인 모델 루트
  노드의 근거다.
- 트레이드오프: PO 기반이었다면 부킹 이전부터 Item 단위 가시성이 가능했겠지만,
  CI/PL 기반에서는 부킹 이후 선적 준비(수출통관·Cut-off 즈음) 단계부터만
  시작된다. 부킹 전 단계는 Item 단위가 아니라 부킹/화물 단위 정보로만 다룬다.
- 운송 모드: 해상(주력) + 항공. 철도/트럭은 내륙구간(Inland Routing)에서만 다룬다.
- Final Destination 도착 이후 W&D의 On-hand 재고와 결합된다. Inbound(+)는
  이 백본을 거쳐 들어오지만, Outbound(−)는 이 백본에 속하지 않는 별도
  흐름(고객 W&D/ERP에서 발생)이면서도 On-hand 재고에 함께 영향을 준다는
  점에서 다이어그램에 나란히 표기한다 — Outbound 자체의 소스·정확도는
  → §9.4. 같은 백본이 Transportation(운송 운영) 관점과 Inventory(재고
  계획) 관점 두 가지로 갈라져 나간다 — §6 참고.

## 6. 정보 구조 (IA)

### 도메인 분리 원칙

같은 Supply Chain Event(부킹→선적→트래킹→하역→Inland→Final Destination)를
서로 다른 관리 단위(Granularity)로 보는 두 개의 View로 나눈다.

| 도메인 | 관리 단위 | 목적 |
|---|---|---|
| **Transportation** | 컨테이너 / B/L (해상), HBL (항공) | 운송 운영 — 어디에 있고, 왜 그런 상태인가 |
| **Inventory** | Item (W&D SKU와 동일 단위) | 재고 계획 — 앞으로 재고가 얼마나 있을 것인가 |

Transportation 데이터를 변경하지 않고 Inventory에서 그대로 재사용한다
(동일 이벤트, 다른 집계 단위일 뿐 별도 데이터 파이프라인이 아님).

### 탭 구조 (2단)

```
Overview  (Transportation + Inventory 통합, 최상위 단일 화면 — 도메인별 개별 Overview 없음)
├── Transportation
│    ├── 화물   (컨테이너/B/L 단위 — 위치·상태·지연 원인·확률적 ETA)
│    ├── 업무   (컨테이너/B/L 단위 — 부킹·서류·통관·Inland Routing 진행)
│    └── 비용   (월별 물류비 현황 + D&D 등 비용 리스크)
└── 재고 (Inventory 도메인)
     ├── 재고 투영 (Projection — Item별 일자별 재고 타임라인, Shortage는 이 안의 필터 뷰)
     └── 입고 예정 (Inbound — Item별 입고 예정 리스트)
          └── Item 상세 (드릴다운 — 별도 탭 아님, 재고 투영/입고 예정에서 클릭 진입)
```

- **Overview 하나만 존재**: Transportation·Inventory 각각에 별도 Overview를 두지
  않고, 최상위 Overview가 두 도메인의 핵심 KPI를 함께 보여준다. 구성 방식
  (설계자 우선순위 선정 vs 사용자 커스터마이즈)은 두 도메인 설계가 끝난 뒤 논의.
- 표기 규칙: 모든 수치에 단위 표기. "총 N건 중 M건" 형태로 전체 대비 이슈
  건수 표시(화물 탭에선 Total/Delayed 형태로 구체화).
- 명칭/톤: 제목줄에 서비스 브랜드명은 노출하지 않는다(브랜드 마크 CI만).
  화면 상단에 노출하는 대시보드 명칭은 **"Proactive Center"**다(브랜드
  마크 옆이 아니라 각 탭 콘텐츠 영역 상단에 표기). 이 규칙 대상이 아닌
  이유와 "Visibility Control Tower" 대신 이 이름을 쓰는 근거는 아래
  참고. 고객사명에 존칭 표현("귀하" 등)은 사용하지 않는다.
  - **명칭 근거**: 이 서비스의 내부 설계 개념/아키텍처는 여전히 §3의
    "Control Tower"(운송 단위 가시성을 Item 단위 가시성으로 잇는
    관제탑)다. 다만 화면에 노출하는 이름은 그 가시성(Visibility) 자체가
    아니라 **가시성이 가능하게 하는 결과**를 강조하기로 했다 — AI를
    도입해 확보하는 가시성은 미래 시점의 상태를 **좀 더 정확하게
    예측하게 하여**, 문제가 실제로 터지기 전에 **선제대응(Proactive
    Response)**할 수 있게 한다. 이 제품이 고객에게 주는
    최종 가치는 "지금 상태를 보여준다"가 아니라 "미리 알고 먼저
    움직이게 해준다"는 것이므로, 화면 명칭도 그 가치를 직접 가리키는
    "Proactive Center"로 정했다. "Visibility"는 이미 하위 탭 명칭(화물
    가시성/업무 가시성/비용 가시성/재고 예측)에 포함돼 있어 대시보드
    이름에 다시 넣으면("Visibility Proactive Center") 중복으로 읽혀
    제외했다.
- 화면 텍스트 언어 규칙: 물류 업계 용어는 영어, 그 외 일반 설명·안내문은
  한글로 쓴다 — 프로젝트 전체 공통 규칙이며 상세 기준은 `CLAUDE.md`에
  있다. 하위 탭 한글 명칭("재고 투영"/"입고 예정")만 이 규칙의 적용
  사례로 여기 남긴다.

## 7. 도착 시점 정확도 (Arrival Accuracy)

Transportation(§8)과 Inventory(§9)는 서로 다른 관리 단위로 같은 사건을
보여주지만, 두 도메인이 공통으로 소비하는 산출값이 하나 있다 — **화물이
언제 도착하는가**다. 화물 탭의 PTA·지연 사유 분석과 재고 탭의 Inbound
인식 시점·신뢰도 설명은 겉보기엔 다른 화면이지만 실은 같은 계산(소스 간
상충 판정 → 도착 시점 확정)을 서로 다른 곳에서 소비하는 것이다. 이 장이
그 계산의 유일한 정본이며, §8·§9는 이 장의 산출값을 화면에 소비만 한다.

### 7.1 ETA / PTA

- `[설계]` **ETA**: 대부분 선사가 공지하는 값 그대로(POL ETD, Carrier ETA
  등)를 쓰지만 전부는 아니다 — 선사 공지가 없거나 늦는 구간은 담당자가
  수기로 확인해 업데이트하는 경우도 있다. 소스가 선사 공지든 수기 입력이든
  화면에 보이는 값은 "ETA"로 동일하게 취급한다(PTA처럼 소스별 신뢰도를
  구분해 보여주지 않음).
- `[가정]` **PTA(Predictive ETA)**: 그래프DB·AIS·항만혼잡도 + 과거 "예측 vs
  실제 도착" 정확도 이력 데이터를 결합한 자체 예측값. P50(중앙값)과
  P95(보수적 기준선) 두 값으로 표현하며, 근거 불명확한 임의의 단일 확률
  수치(예: "72%")는 사용하지 않는다. 선사·항로·선박타입별 "과거 예측 정확도
  이력" 데이터 확보가 전제이며 아직 미확보다(→ §11).
- PTA는 Main-carriage 구간에만 적용. 선적 전은 POL ETD, 하역 후는 FDEST
  ETA가 기준.
- FDEST ETA는 Inbound 인식 시점의 입력값이다(→ §7.3).

### 7.2 지연 사유 분석

(구 "지연 계보도/Delay Lineage")

핵심은 단순하다 — **서로 상충하는 소스가 있을 때, 이번 건 상황에 더
부합하는 정황 증거를 가진 쪽을 신뢰하자**는 것이다. 지연 사유 분석은
그 정황 증거를 모으는 절차이며, 화면에 보이는 "지연 원인 서술"은 그
판단의 부산물이지 목적이 아니다.

- **목적**: 지연 원인을 서술하는 것 자체가 최종 목표가 아니다. 화물이 언제
  창고에 도착할지(→ §7.3)를 알려주는 소스는 여러 개(선사 milestone, AIS
  기반 예측 벤더 등)이고 서로 값이 다를 때가 있다. 지연 사유 분석의 실제
  역할은 이런 상충 상황에서 **이번 건은 어느 소스를 더 신뢰해야 하는지
  판단하는 정황 증거(corroborating evidence)를 모으는 것**이다. 그 판단
  결과가 §7.1 PTA와 §7.3 Inbound 인식 시점의 정확도로 직결된다.
- **2단 구조** (§7.5 Inbound 신뢰도 설명 Agent도 이 구조를 그대로 재사용):
  1. `[가정]` 정적 신뢰도 — 소스별·항로별·선사별 과거 예측 정확도 이력
     (누적 오차 통계)로 계산하는 베이스라인 가중치. "이 소스는 평소
     얼마나 맞았나"를 미리 계산해두는 것이라 순수 통계이며 AI가 필요
     없다. 이력 데이터는 아직 미확보(→ §11).
  2. `[설계]` 건별 보정 — 이번 건에서 소스 간 값이 상충할 때, 항만 혼잡도·
     뉴스 등 독립적인 신호가 어느 쪽 소스를 뒷받침하는지 확인해 신뢰도를
     동적으로 조정한다("이번 건은 상황이 이러니 이 소스가 더 맞을 것").
     정형화되지 않은 여러 신호를 엮어 판단해야 하므로 AI/LLM 추론이
     실제로 필요한 지점은 이 단계뿐이다.
- `[설계]` 화면상 시각화 형태(화물 탭): 그래프/체인형으로 확정(타임라인/
  스텝형은 탈락). 예: 컨테이너 지연 → 선박 3일 지연 → 부산항 접안 대기 →
  항만 혼잡도 "높음". 이 서술은 화면의 핵심 콘텐츠가 아니라 최종 산출물
  (신뢰도 조정된 도착일)의 근거·투명성 표시로 쓰인다 — 화면 배치는 §8.1.
- 환적 있는 경우: 환적선 스케줄 미확정 → 지연은 아니나 리스크로 표시(단,
  "리스크" 판정 기준 자체는 미정 — → §11).

### 7.3 Inbound 인식 시점 및 반영 기준

FDEST 관련 날짜는 다음 순서로 정의한다: On-board 시점의 **Initial FDEST
ETA**(최초 예상) → 운송 진행에 따라 갱신되는 **현재 FDEST ETA** → 실제
최종 도착인 **FDEST ATA**. Initial FDEST ETA는 최초 예상과 현재 예상의
변화 확인(지연 여부 비교)에만 활용한다.

**어느 값을 Inbound 인식 시점으로 쓰는가**
- `[설계]` **현재 MVP**: raw data에 있는 "현재 FDEST ETA" 단일값을 그대로
  쓴다. PTA를 구성하는 확률 모델(그래프DB·AIS·과거 예측정확도 이력) 자체가
  아직 없기 때문이며, 확률적 표현을 배제하기로 한 설계 결정이 아니라 데이터
  한계에 따른 임시 대체다.
- `[가정]` **Target 설계**: §7.1의 PTA(P50/P95)가 고도화되면 Inbound 인식
  시점도 그 보수적 기준선인 **P95**를 쓰는 것이 맞는 방향이다 — 화물 탭과
  같은 신뢰도 체계를 공유해야 앞뒤가 맞는다.
- **P95 채택 시 주의할 리스크**: P95는 보수적(늦은 쪽) 기준이라 실제 도착이
  그보다 빠르면 이미 들어온 재고가 "미입고"로 표시되는 구간이 생기고, 이를
  보고 추가 발주하면 재고가 필요 이상으로 쌓일 수 있다. MVP에서는 P50/P95
  듀얼 표시로 분리하지 않고(숫자를 이원화하면 §9.5의 "결과부터 단순하게
  보여준다"는 목적과 충돌) "실제 도착은 이보다 빠를 수 있다"는 안내 문구로만
  처리한다. 듀얼 표시는 PTA 모델이 실제로 고도화되면 재검토할 미래 과제다.

**Item별 도착일 산출** `[설계]`

| 조건 | 계산 방식 |
|---|---|
| Container-Item Mapping 존재 | Container의 **현재 FDEST ETA**(내륙운송 포함 최종 목적지 도착 기준) |
| Container-Item Mapping 미존재 | 동일 B/L 내 Container 중 **가장 늦은 FDEST ETA** |

**어느 물량부터 반영하는가** `[설계]`

**On-board 확인 물량부터 Inbound로 반영**한다. On-board 이전의
Booking/Pre-carriage 물량은 아직 실제 해당 선박에 선적될지 불확실하고 ETA
정확도도 낮기 때문에, Projected Inventory에 바로 포함해 잘못된 재고 안정
신호를 주지 않는다 — 대신 별도의 Pre-carriage/Booked Pipeline 정보로 제공할
수 있다(Item, Planned Qty, Expected POL ETD, Planned Vessel, "On-board:
Not Confirmed"). Vessel Departure 이전(Booking/Stuffing/Gate-In 단계)에는
Projected Inventory 계산을 시작하지 않고 업무 진행 현황만 제공한다.

향후 필요하면 다음 두 Projection을 분리해 제공할 수 있다(현재는 Confirmed
Projection만 계산하며, 두 개를 하나의 재고 숫자로 혼합하지 않는 것이 원칙):

```
Confirmed Projection = On-hand + On-board Inbound − Outbound
Planning Projection   = Confirmed Projection + Pre-carriage/Booked Pipeline
```

**FDEST 도착 → 실제 창고 입고 버퍼**
- `[검증]` raw data에 실입고일(**W/H In Date**) 필드가 실제로 존재함을 확인했다.
- `[설계]` 우선순위: W/H In Date가 있으면 1순위, 없으면 FDEST ATA + 버퍼
  (2순위), 그마저 없으면 FDEST ETA + 버퍼(3순위)로 추정한다.
- `[가정]` 버퍼의 구체적 일수는 미확정이다 — 확보한 raw data 스냅샷에
  완료건(FDEST ATA 존재)이 0건이라 실측 검증이 불가능한 상태다(→ §11).

### 7.4 ETA Confidence

`[구상]` ETA에는 날짜 하나만이 아니라 "현재 시점에서 이 ETA를 얼마나 신뢰할
수 있는가"를 함께 전달하는 Confidence 개념이 필요하다. MVP에서 복잡한
Confidence 모델을 반드시 구현할 필요는 없다. 기본 개념: Booking/
Pre-carriage(Provisional/낮은 신뢰) → On-board(FDEST ETA 계산의 본격 시작)
→ 운송 진행(ETA 지속 업데이트) → POD/FDEST 접근(정확도 상승) → FDEST(Actual).
실제 Confidence는 Route·TS·Port 변동성·운송 단계 등 다양한 요소를 반영해
향후 발전시킬 수 있다. 선사 ETA가 선박 위치·스케줄로 볼 때 비합리적으로
보일 때 자체적으로 수정하는 것은 장기적으로 필요하나 MVP에서는 다루지
않는다(→ §11).

**미해결 — "신뢰할 수 있다"의 판단 근거 자체가 정의되지 않았다**: 위 5단계는
운송 진행 정도에 따른 신뢰도의 방향성일 뿐, 무엇을 근거로 특정 ETA를
"신뢰할 만하다"고 판단할지는 아직 정의가 없다. 과거 갭(예상 대비 변동폭)
분포와 비교해 "이례적이지 않다"는 것과 "그래서 이 숫자가 정확하다"는 것은
서로 다른 주장이다 — 전자는 소극적 사실(반증 없음)이고, 후자는 적극적
corroboration(예: 선사의 실제 갱신 공지, 실측 항해 데이터)이 있어야 성립하는
주장이다. §7.5가 이 구분을 명확히 하지 못했던 사례는 → §7.5 "당겨짐 케이스".
신뢰도 판단 기준의 정의와, 그 기준을 뒷받침할 데이터가 무엇인지부터 정해야
한다(→ §11).

### 7.5 Inbound 신뢰도 설명 Agent (다음 구현 대상)

재고 탭에서 실제로 AI 설명이 필요한 값은 하나뿐이다 — Inbound 누적의 기준이
되는 FDEST ETA(→ §7.3)를 얼마나 신뢰할 수 있는지, 밀릴 위험 요인이 뭔지.
On-hand/Outbound는 이미 주어진 입력값이라 AI가 붙을 지점이 없다. 이를
설명하는 것이 §9.7 MVP 가설을 직접 검증하는 기능이다. §7.2 지연 사유 분석의
2단 구조(정적 신뢰도 + 건별 보정)를 그대로 재사용한다.

**스코프 — 전체 Inbound에 적용, Shortage/Risk는 강조만** `[검증]`
분석 파이프라인은 Item의 Normal/Risk/Shortage 상태와 무관하다 — Vessel+POD를
아래 3-소스에 조인하는 계산은 모든 Inbound 화물에 동일하게 적용 가능하므로
Shortage Item에만 한정할 기술적 이유가 없다. 따라서 적용 대상은 §9.5 "입고
예정" 리스트 전체이고, Shortage/Risk Item에 걸린 Inbound(및 그 Item의 회복
시점을 만드는 대표 Inbound)는 화면에서 강조 표시만 한다(우선순위 노출이지
범위 제한이 아님).

**실행 트리거** `[설계]`
적용 범위와 달리 **실행 시점**은 Item 상태로 구분한다. Shortage Item은 자동
실행(가장 급한 케이스라 화면을 열자마자 근거가 준비돼 있어야 함), Normal/Risk는
사용자가 드릴다운을 열었을 때 그 건만 on-demand 실행한다 — 전 건을 상시
자동 분석하면 WebSearch 등 비용 호출이 불필요하게 늘어나기 때문이다.

**3-소스 조합** `[검증]` (실 raw data 조인 검증 완료)

| 소스 | 역할 | 검증 결과 |
|---|---|---|
| `CP_Vessel_List.xlsx` | 선박 실측 Gap/Risk Alert | `Tracking.xlsx` 155건을 Current Vessel+POD Port로 조인해 126건(81%) 매칭. 미매칭 29건은 fallback 필요 |
| `Port Congestion.xlsx` | 항만 혼잡 추세 | 실제 POD 항구(MXZLO/MXLZC) 2곳 모두 주간 Dwell/Waiting 추세 커버 |
| WebSearch | 정성적 원인(통관 파업, 트럭커 봉쇄 등) | 별도 크롤링 없이 확보 가능. 단 결과의 기준일(as-of date)이 제각각이고 표기 자체가 없는 경우도 있어 신선도 검증이 필수 |
| ~~`Incident List.xlsx`~~ | — | 파일 자체는 실데이터(HBL 6,829건)이나 커버 항로(PH/GB/GH 등)가 이번 Tracking 항로(KR/CN/ID→MX)와 겹치지 않아 이번 데이터셋엔 미사용 |

WebSearch 결과는 **기준일을 추출해 함께 표시하고, 2주 이상 지난 정보는
신뢰도를 낮추거나 배제**한다. 기사 유형별 정확한 임계값은 미정(→ §11).

**산출** `[설계]`
1. 이 Inbound 건이 왜 지연/정상인지 근거 텍스트.
2. FDEST ETA의 신뢰도 — 밀릴 위험 요인이 있으면 명시.
3. FDEST ETA가 Init ETA 대비 크게 당겨진 경우, 과거에 유사한 폭의 변동이
   있었는지 참고 정보(아래 참고 — 신뢰도를 확정하는 근거는 아님).

**당겨짐(조기 확정) 케이스** `[가정]`
FDEST ETA는 지연뿐 아니라 Init ETA보다 크게 당겨지는 경우도 많다(실 데이터
155건 중 87건, 56%). 이 경우 "왜 당겨졌는지" 메커니즘을 추측해 설명하지
않는다 — 원인이 확보한 raw data 밖의 정보라 근거 없는 추측이 되기 때문이다.

과거 갭(FDEST ETA − Init ETA) 분포와 비교하는 참고 정보를 보여줄 수는
있다. 단 이 비교의 집계 단위는 **Item이 아니라 컨테이너/B/L 단위로 중복
제거한 뒤 노선(POD→F.DEST)·선사** 기준이어야 한다 — 갭은 Item의 속성이
아니라 그 화물이 실제로 실린 운송 단위(컨테이너/B/L)가 움직인 결과이기
때문이다. Item 단위로 집계하면 표본이 우연히 특정 노선·선사에 몰려 있을 때
"이 Item은 원래 그렇다"는 착시가 생긴다(프로토타입 검증 중 실제로 발견된
문제 — 상세는 `PROGRESS.md` 참고).

**이 비교가 증명하는 것과 증명하지 못하는 것을 구분해야 한다.** 과거에도
이런 폭의 변동이 있었다는 것(통계적으로 이례적이지 않음)은 이번 값에 대한
**반증이 없다는 소극적 사실**일 뿐이며, "그래서 이번 FDEST ETA가 정확하다"는
**적극적 확증이 아니다**. 화면 문구도 "정상 판정"·"신뢰할 만함"처럼 확증하는
톤을 쓰지 않고, 이 수준(반증 없음)에 맞는 표현으로 낮춰야 한다. 또한 표본이
작은 노선·선사 조합에서는 "1 표준편차 이내"라는 임계값 자체가 통계적으로
느슨해지므로(표본이 작을수록 우연히 범위 안에 들기 쉬움), 최소 표본 수
기준 없이 이 판정을 쓰면 안 된다 — 최소 표본 수는 미정(→ §11).

**미해결**: 이 절 전체가 결국 §7.4에서 이미 지적한 문제로 귀결된다 — "ETA
신뢰도"를 판단하는 근거 자체가 아직 정의돼 있지 않다(→ §7.4). 이 문서의
현재 버전은 그 정의가 없는 상태에서 갭 통계를 신뢰도의 대리 지표처럼
서술했던 오류가 있었고, 이번에 바로잡았다.

**원칙**: AI가 FDEST ETA 숫자 자체를 임의로 바꾸지 않는다(→ §9.6). 신뢰도·
위험 요인을 "설명"하는 데 그치고, Projected Inventory 계산은 §7.3/§9.4의
결정론적 로직을 그대로 쓴다.

## 8. Transportation 기능요구사항

### 8.1 화물 탭 — 어디에 있고, 언제 도착할 것인가

**목적**: 화물 상태·위치를 컨테이너/B/L 단위로 한 화면에서 보여준다. 도착
시점 정확도의 방법론(ETA/PTA, 지연 사유 분석)은 → §7. Item 단위 재고
투영은 Inventory 도메인 소관(→ §9).

**리스트 구조** `[설계]`
- 리스트 단위: 해상=컨테이너(없으면 HBL), 항공=HBL. 컨테이너 번호는 11자리
  붙여쓰기, 하이픈 없음(예: `TCLU5520134`).
- 해상/항공은 한 리스트로 섞지 않고 같은 화면에 순차 배치(해상 섹션 → 항공
  섹션). 상단 "Jump to Sea/Air"는 필터가 아니라 스크롤 이동만 수행.
- 상단 요약 통계는 해상+항공 합산과 모드별 분리를 함께 표시.
- 리스트 첫 컬럼: Container No(굵게) 아래 BL No, PO No 서브라인(LCL/HBL은
  ID 자체가 BL이라 PO No만). PO No는 CI/PL에 찍힌 참조 라벨일 뿐이다(→ §5).
  우측은 POL → POD 구간으로 표기(PO 컬럼 중복 표기 안 함).
- 좌측 상단: 단위 토글(Container/HBL). 우측 상단: Search(B/L·Container·
  PO No 통합 검색). Item 토글은 Inventory 도메인으로 이동했다.

**상태 3단계** `[설계]` (탭 전환 시 컬럼 자체가 바뀜, 행 필터링 아님)

| 단계 | 표시 컬럼 |
|---|---|
| Pre-carriage | 예정 Vessel/Flight, POL ETD, TS(예정) |
| Main-carriage | 현재 Vessel/Flight, 도착예정(POD) Vessel/Flight, TS, Carrier ETA/PTA |
| On-carriage | FDEST ETA, D&D LFD |

**지연 표시** `[설계]`
- 지연 유형을 선적 지연 / 운송 지연 / 도착 지연으로 구간별 분리.
- 상단 요약과 각 섹션 모두 Total/Delayed 병기(Delayed는 빨간색 강조).
- TS(환적) 횟수는 해상·항공 Pre-carriage/Main-carriage 단계에서 칩으로
  표기(On-carriage는 이미 환적이 종료된 시점이라 표기 대상 아님).

**선박 위치 연동** `[검증]`
- 선박명 클릭 → 실제 AIS 위치를 지도에 표시(`vessel-tracker/` 프로토타입,
  → §10). 항공 편명은 AIS 대상이 아니라 미연동.

### 8.2 업무 탭 — 현재 어느 단계에 있고, 업무 지연은 없는가 (신규 개척 영역)

**목적**: 부킹부터 서류·통관·Inland Routing까지 능동적 행정 액션(제출·승인·
결정)의 진행 상황을 추적하고 실행까지 연결한다.

**분류 기준**: 업무 탭 = 능동적 행정 액션, 화물 탭 = 그 결과로 발생하는 물리적
이동/위치. (예: 게이트인/On-board/공컨테이너 픽업은 화물 탭 소관, B/L 발급은
서류이므로 업무 탭 소관.)

**리스트 구조 — 1단, 부킹이 최상위** `[설계]`
- 최상위이자 유일한 리스트는 **부킹 리스트**다. PO를 별도 엔티티로 추적하지
  않는 이유는 → §5. 포워더가 아는 첫 이벤트는 부킹 요청이므로, 부킹 이전
  단계에 "아직 부킹 안 된 PO 목록"을 보여주는 입구 화면은 성립하지 않는다.
- 컬럼: 부킹No, 관련 PO No(서브라인 참고 표시), Item/수량(부킹 시점엔
  "미확정", Cut-off 준비 중 CI/PL 확보 시 채워짐 — 추출 방식은 → §8.5),
  부킹 상태(요청/확정/롤오버), Cut-off(SI/VGM/CY 통합 카운트다운), 수출통관,
  B/L 발급.
- 콘솔리데이션(여러 PO가 한 부킹으로 묶임)·분할선적(한 PO가 여러 부킹으로
  쪼개짐) 관계는 CI/PL에 찍힌 PO 참조번호를 부킹 행 아래 여러 줄로 나열하는
  방식으로만 보여준다(실체 추적이 아니라 표시상의 참고 정보).

**부킹 상태** `[설계]`
- 순서: 요청 → 확정 → (예외) 롤오버 → 재확정 → …
- 롤오버는 "대기 상태"가 아니라 이미 확정된 부킹이 선사 사정(스페이스
  부족·blank sailing 등)으로 다음 항차로 밀려나는 **예외 이벤트**다.

**Cut-off (SI/VGM/CY)** `[설계]`
- 부킹 단위(같은 sailing에 걸린 화물 전체에 동일 시각 적용)로 서로 다른
  시점을 하나의 카운트다운 타임라인으로 통합 표시.
- VGM만 컨테이너별 개별 제출 필요 → "VGM 3/5 제출완료" 식 진행률 + 단위
  표기("컨테이너") + 설명 툴팁 + 하단 각주.
- 롤오버 발생 시 해당 부킹의 cut-off 세트 전체가 새 sailing 기준으로 리셋되며,
  화면에 이를 표시해야 함.

**섹션 2분할** `[설계]`
- **선적 준비**: 부킹 → Cut-off → 수출통관(CI/PL 확보로 Item 확정) → B/L
  (Pre-carriage 시점).
- **도착 준비**: 수입통관 → Inland Routing (Main-carriage 후반~On-carriage 시점).

**도착 준비 상세**
- `[설계]` 화물 탭과 동일 컨테이너/HBL 레코드의 필터+액션 뷰(별도 데이터
  아님). 화물 탭에 이미 있는 FDEST ETA/D&D LFD는 중복 표시하지 않고,
  통관상태·Inland Routing처럼 화물 탭에 없는 컬럼만 추가.
- `[설계]` 노출 기간: 기본 POD ETA 기준 D-14 이내(철도의 더 이른 리드타임까지
  커버), 사용자가 기간 좁혀보기 가능. 리드타임 참고 — 드레이지(트럭)는 POD
  도착 5~7일 전 예약 권장, 철도는 스케줄 고정이라 7~10일+ 필요할 수 있어
  POD ETA 기준 D-7 트리거로 섹션 활성화를 제안.
- `[설계]` 수입통관은 화물마다 필수 여부가 다름 → "통관 필요여부/상태"
  (해당없음/진행중/완료) 컬럼. 필요한 화물은 통관 미완료 시 Inland Routing
  액션 잠김.
- `[가정]` Inland Routing 옵션 비교(리드타임/비용/프리타임)는 트럭/철도
  스케줄 마스터 데이터가 등록돼 있어야 성립한다(→ §11).
- 역방향 연동(정정): Inland Routing 실행 결과가 곧바로 FDEST ETA에
  반영되는 게 아니라, 그 사이에 **Appointment(화주 인도 약속일) 확정**
  단계가 있다 — 트럭/철도 선택·터미널 반출 가능일 등을 근거로 화주와
  Appointment를 합의하면, 그 값이 화물 탭 FDEST ETA 재계산 입력값으로
  반영된다(같은 레코드라 별도 동기화 불필요). 실제 운송이 Appointment와
  달라지면(지연 등) 그때 다시 갱신된다. 상세는 아래 "도착 준비 —
  모니터링 및 Inland Routing 실행 지원" 항목 6 참고. 이 FDEST ETA는
  Inbound 인식 시점의 입력값이다(→ §7.3).

**도착 준비 — 모니터링 및 Inland Routing 실행 지원** `[구상]`
- "도착 준비" 섹션(수입통관 → Inland Routing, Main-carriage 후반~
  On-carriage)에 필요한 정보를 한 화면에 모아 보여준다. **Port 도착 전
  모니터링**과 **Inland Routing 실행(도착 이후)** 두 단계로 나뉘며,
  아래 항목은 전부 신규 논의로 아직 화면 설계 전 단계다.

  **① 도착 모니터링 (Port 도착 전~도착 시점)**
  1. **선박 현재 위치·PTA 모니터링**: 화물 탭·§8.1에서 이미 다루는
     AIS 위치 연동·PTA(P50/P95) 개념을 이 화면에서도 재사용(별도 로직
     아님) — 도착 임박 화물의 PTA를 Inland Routing 예약 시점 판단
     근거로 다시 노출하는 구성.
  2. **Port 혼잡도**: §7.1 그래프DB 구성요소로 PTA를 보정하는 내부
     입력값으로 이미 다루고 있음 — 별도의 독립 지표(예상 하역 완료
     시점 등)를 새로 만들지 않고, 기존 PTA에 이미 반영된 정보를 그대로
     참고하는 것으로 범위를 좁힘.
  3. **터미널 Free Time·체화료 모니터링**: §8.3 비용 탭의 D&D 통합
     리스트(임박→1주→2주→3주→4주 이상)와 **같은 데이터**를 참조
     정보로 재사용한다(별도 구축 아님) — 해당 컨테이너의 D&D 상태를
     요약 형태로 끌어와 보여주는 구성(비용 탭과 동일한 원칙 — §8.2
     "도착 준비 상세" 서두의 "같은 레코드의 필터+액션 뷰" 원칙 확장).

  **② Inland Routing 실행 (도착 이후)**
  4. **터미널 반출 제약사항** `[옵션 적용]`: 샤시(Chassis) 부족, 배정
     운송사 Capa 초과 등으로 예약해도 실제 반출이 막히는 경우가 있는지
     확인. "옵션"은 기능 자체의 우선순위가 낮다는 뜻이 아니라, **이런
     제약을 관리하는 포트·터미널에서만 켜지는 조건부 기능**이라는 뜻 —
     모든 포트·터미널이 이 데이터를 갖고 있지 않으므로, 데이터가 있는
     구간에서만 노출한다. 현재 PRD에 이런 제약 데이터를 다루는 곳이
     없어 신규 데이터 소스가 필요 — 아래 "정리 필요 사항" 참고.
  5. **철도 운송 가능여부·비용**: 기존 "트럭/철도 스케줄 마스터 데이터"
     가정(→ §11 미해결 이슈)에 **비용(운임) 비교**를 명시적으로
     추가. 리드타임만이 아니라 트럭 대비 철도의 비용 차이까지 옵션
     비교 화면에 포함되어야 함.
  6. **Appointment(화주 인도 약속일) 확정**: 위 ①·② 항목에서 모은
     정보를 근거로 화주(Cnee)와 "언제 가져다주겠다"고 합의하는 날짜.
     인과 순서는 **①·② 정보 확인 → Appointment로 화주와 확정 → 그
     Appointment대로 실제 운송 실행 → 그 결과가 FDEST ETA에 반영**이다
     — 기존 서술("Inland Routing 실행 결과가 FDEST ETA 재계산 입력값")은
     Appointment라는 중간 단계 없이 실행 결과가 곧바로 FDEST ETA에
     반영되는 것처럼 읽혀 부정확했다. FDEST ETA는 Appointment 확정
     시점부터는 그 값을 기준으로 갱신되고, 실제 운송이 Appointment와
     달라지면(지연 등) 그때 다시 갱신되는 구조로 정정한다.

**스페이스 타이트 신호** `[설계]`
- 선사-Pantos 1:1 계약 구조상 고객별 정확한 allocation 잔여는 노출 불가·비선호.
  대신 Pantos 항로별 부킹 성공률/거절률 기반 타이트 정도 신호로 대체한다.
- 표현 방식: 절대 수치가 아니라 **등급(원활~매우타이트) + 추세 화살표**
  (Freightos Terminal Booking/Supply Index 참고, 정밀 퍼센트 지양).

**Port-to-Port 스케줄 조회** `[설계]`
- 별도 컴포넌트, 진입점 2곳:
  1. 기존 부킹 상세 행의 "스케줄 조회" 액션 — 해당 부킹의 POL/POD 프리필
     (주로 롤오버 등으로 대체 스케줄을 재확인할 때 사용).
  2. 업무 탭 상단 고정 진입점 — 특정 부킹과 무관하게 독립 조회, 결과에
     "이 스케줄로 부킹 요청" 액션이 붙어 신규 부킹 생성으로 이어짐.
- 입력: POL/POD/기간. 결과: 선사·선박·POL ETD·POD ETA·Lead Time·TS·탄소배출.
- 탄소배출 축은 §8.4 대안 루트 추천과 동일 축 재사용, 추후 연결.
- Overview 진입점 추가는 Overview 재구성 시점으로 보류.

### 8.3 비용 탭 — 불필요한 비용이 발생하고 있지는 않는가

**월별 물류비 현황** `[설계]`
- 항목별(운임/D&D/기타) 구성 + 물동량 대비 비용 추이를 함께 보여주는
  현황형 콘텐츠 — "현재 발생분/추이"를 보는 화면.
- 목업 반영 완료(스택 바 차트 + 물동량 라인 오버레이 + 항목별 breakdown 테이블).

**D&D(체화료·반납지체)** `[설계]`
- 월별 현황과 성격이 다른 사전 알림형 콘텐츠(예정/진행중 상태 알림). Free
  Time 임박과 체화료 발생중을 하나의 리스트로 통합해, 리스크 진행 순서
  (임박 → 1주 이내 → 2주 이내 → 3주 이내 → 4주 이상)로 표시한다. 구간이
  길어질수록 리스크가 커지므로 4주 이상 구간은 강조 표시. 목업 반영 완료.

**PCS(항만혼잡할증)** `[구상]`
- 확정 항목에서 제외하고 후보로 재분류했다. 이유: (1) 선사 공지가
  비정형·선사별로 상이해 크롤링 구현·유지보수 부담이 크고, (2) 상시
  비용이 아니라 특정 항로·시기에만 발생하는 이벤트성 비용이라 대부분
  고객·달엔 해당이 없다. 전용 알림/카드 없이, 발생 시 월별 물류비 현황의
  항목 중 하나로만 반영한다.

- 그 외 후보(확정 항목 아님): 할증료 일반, 서류오류 정정료, 롤오버
  간접비용, CFS 보관료, FX — 담당자 체감상 우선순위 근거 미확보.

### 8.4 대안 루트 추천 (사고 발생 시)

`[구상]` — 콘셉트 정의만 된 상태, 설계 착수 전.

- 리드타임/운송모드/탄소배출 등 다축으로 평가해 대안을 제시.
- 판단 로직 자체보다 **비교 대상 데이터(대안 스케줄, 실시간 선복, 운임,
  탄소계수) 확보**가 핵심 과제 — "기능 개발"이 아니라 "데이터/제휴 확보"
  문제로 정의(→ §11).

### 8.5 CI/PL Intake — Item/QTY 추출 (신규 AI 기능 후보)

**배경**: Item 출처는 CI/PL이다(→ §5, §9.1). CI/PL 원본이 실제로 어떤 형태로
들어오는지(구조화 파일 vs 이미지/스캔)는 미확인이며 둘 다 실제로 가능하므로
하나로 단정하지 않는다(→ §11). 두 형태 모두를 다루되, 최종적으로 두 경로
모두 같은 표준 스키마(Item, QTY, PO No 참조용, HS Code, Container No)로
정규화해 §9.1 Item 엔티티에 연결한다.

**경로 A — 구조화 파일(Excel/CSV)** `[설계]`
- 입수: 운영담당자가 이메일로 받은 CI/PL을 지정 폴더(또는 웹 업로드 화면)에
  업로드.
- 파싱: 워크북 로드 자체는 결정론적 로직(AI 불필요).
- **컬럼 매핑에만 AI 필요**: 고객마다 Item을 부르는 컬럼명이 다름(`Model`,
  `Item Description`, `품명` 등) — 헤더 행 + 샘플 행을 LLM에 주고 Item/QTY/
  PO No 등 표준 필드에 매핑. 고객사당 최초 1회만 수행하고 매핑 규칙을 저장해
  이후는 룰 기반으로 재사용.
- 검수: 운영담당자가 매핑 결과를 확인·승인하는 화면 필요(특히 최초 등록 시).

**경로 B — 이미지/PDF/스캔본** `[설계]`
- 입수: 같은 업로드 폴더에 이미지/PDF로 들어오면 MIME 타입으로 자동 분기.
- 추출: 프로토타입 단계는 멀티모달 LLM으로 직접 추출 시도 가능(별도 계약
  불필요). 프로덕션 스케일에서는 전용 Document AI(예: AWS Textract, Azure
  Document Intelligence)를 전단에 두고 LLM은 구조화·검증만 담당하는 하이브리드
  고려.
- 신뢰도 낮은 필드는 강조 표시해 **사람 검수 필수** — 특히 QTY는 재고 투영
  계산에 직결되므로 완전 자동화하지 않는다.

**공통 원칙**: AI는 "컬럼 매핑 판단"·"이미지에서 필드 추출"이라는 좁은
지점에만 관여하고 그 결과는 사람 검수를 거친다(→ §9.6). 구현 형태는
`vessel-tracker/`처럼 목업과 분리된 별도 백엔드 프로세스(파일 워처 +
파서/OCR 호출)가 적합하다.

**미정** `[가정]`: 업로드 폴더가 로컬 네트워크 폴더인지 별도 웹 UI인지,
Pantos 실제 CI/PL 입수 경로가 A/B 중 무엇인지(또는 섞여 있는지) — 운영
담당자 워크플로우 확인이 선행돼야 한다(→ §11).

## 9. Inventory 기능요구사항

**목적**: WMS를 대체하지 않고, Transportation 이벤트와 W&D 재고 정보를
Item 단위로 결합하여 미래 재고(Projected Inventory)와 예상 Shortage를
보여주는 재고 계획(Planning) 뷰. Transportation이 "운송 운영" 관점이라면
Inventory는 "재고 계획" 관점 — 같은 이벤트를 다른 단위(Item)로 재구성한 것.

> **책임 경계**: Projected Inventory를 이루는 네 변수(On-hand·Inbound·
> Outbound·Safety Stock) 중 On-hand·Outbound·Safety Stock은 고객 시스템
> (W&D/ERP)에서 주어지는 입력값이며 본 서비스가 그 정확도를 개선하지
> 않는다. 본 서비스가 정확도를 책임지는 변수는 **Inbound 시점 하나**이고,
> 그 산출 방법론이 §7이다.

장기적으로는 Shortage 발생 시 기존 Supply Pipeline과 신규 조달 가능성을
함께 검토해 고객의 발주 의사결정까지 지원하는 것이 방향이지만, 실제 PO
생성·공급처 전달·Booking 등 발주 실행 자체는 범위 밖이다(→ §12).

### 9.1 도메인 모델

Item의 출처는 PO가 아니라 **Commercial Invoice(CI) / Packing List(PL)**다
(→ §5).

```
Commercial Invoice / Packing List  (Item 출처 — PO No는 참조 라벨로만 포함)
 └── Item ──(Container-Item Mapping, N:M)── Container
      │
      ├── B/L                 (Item이 속한 선적 단위)
      └── W&D Inventory       (Item의 On-hand)
```

- Item은 W&D SKU와 동일한 관리 단위(Granularity)를 가지는 표준 Business
  Object다. 이 문서에서 `Item`은 언제나 이 단위를 가리킨다.
- **Item ↔ Container는 N:M** — Container-Item Mapping이라는 별도 연결
  엔티티로 표현한다(한 Item이 여러 Container·여러 화물에 걸쳐 존재할 수 있음).
  Mapping이 없는 경우 §7.3의 정책에 따라 보수적으로 대체한다.
- PO는 별도 엔티티로 추적하지 않는다(→ §5). CI/PL에 참조번호가 찍혀 있으면
  Item 상세에 참고용 라벨로만 노출한다.
- 현재 확보한 샘플 고객 데이터(`raw data/Tracking.xlsx`)에서는 Item이 `Model`
  컬럼, 수량이 `QTY` 컬럼으로 표현된다.

### 9.2 데이터 소스

**Transportation(Forwarding)** — 아래는 현재 시스템에 존재하는 데이터
전체가 아니라, 파일럿 검증에 필요하다고 판단해 선별한 항목이다:
- `[검증]` B/L 및 Container가 포함된 Tracking 정보, Item 정보, Item별 실제 수량
- `[검증]` POD ETA, Initial FDEST ETA, FDEST ETA, FDEST ATA
- `[검증]` Vessel Calling Port Schedule, PCP/CP로 활용 가능한 Calling 정보
- `[검증]` 주요 Port의 최근 주차별 Dwell Days/Congestion 정보
- `[가정]` Vessel·Port 관련 News 데이터 — 실제 연결 방식 미정(→ §11)

**W&D**:

| 구분 | 소스 | 성숙도 | 비고 |
|---|---|---|---|
| On-hand | W&D | `[가정]` | 특정 스냅샷 시점(예: 기준일 0시) 재고, 연동 방식 미정 |
| Inbound | Transportation | `[검증]` | FDEST ETA 재사용 (→ §7.3) |
| Outbound | 고객 ERP/W&D (또는 그에 준하는 출고 계획 값) | `[가정]` | 소스를 특정 시스템으로 고정하지 않는다 (→ §9.4) |
| Safety Stock | 고객 ERP/W&D 마스터 데이터 | `[가정]` | Item별 등록값, 미등록 Item은 강조 표시 대상에서 제외 |

플랫폼은 수요를 생성하지 않고, 위 입력 데이터를 이용해 미래 재고를 계산만
한다.

> **합성데이터 방침(정본)**: MVP에서 W&D 실데이터를 확보하지 못하는 경우
> 합성데이터(Synthetic Data, `synthetic-data/`)를 사용하며, 실제 데이터와
> 합성데이터는 화면에서 구분 가능하도록 관리한다(`source_type=SYNTHETIC` 등).

Container, Vessel, Port는 하나의 종속 계층이 아니라 각각 독립적인 Entity로
보고 필요한 관계를 연결한다. Port Congestion이 FDEST ETA에 직접 영향을
준다고 단순하게 판단하지 않으며, 기본 영향 관계는 다음 순서로 본다: Port
Congestion/이슈 → Calling Port(CP) → Vessel Schedule → POD ETA → POD
이후 운송 일정 → FDEST ETA → Item Inbound Date → Projected Inventory.
MVP에서는 Port Congestion이나 News가 ETA 변화의 직접적인 원인이라고 자동
단정하지 않는다(정량적 인과모델은 미정 — → §11).

### 9.3 Inbound 인식 시점 (계산 참조)

Item별 Inbound 인식 시점을 정하는 방법론(어느 ETA 값을 쓸지, On-board
기준, 창고 입고 버퍼)은 §7.3을 따른다. 여기서는 §9.4의 Item×Date 합산에
바로 쓰는 Container-Item Mapping 계산표만 참조용으로 둔다.

| 조건 | 계산 방식 |
|---|---|
| Container-Item Mapping 존재 | Container의 **현재 FDEST ETA**(내륙운송 포함 최종 목적지 도착 기준) |
| Container-Item Mapping 미존재 | 동일 B/L 내 Container 중 **가장 늦은 FDEST ETA** |

### 9.4 재고 계산 및 상태 판정

```
Projected Inventory(t) = On-hand(기준일) + Σ Inbound(≤t) − Σ Outbound(≤t)
```

- `[설계]` 계산은 **Item × Date** 단위로 합산한다 — 같은 Item이 여러 B/L·
  Container·Shipment에 걸쳐 있어도 동일한 FDEST ETA 날짜에 도착하면 하나의
  Inbound 수량으로 더한다. 원래 shipment별 근거는 드릴다운(→ §9.5)으로 확인.
- `[설계]` 일자별 누적 계산이며, 단일 스냅샷 값이 아니라 §9.5 재고 투영
  타임라인 전체에 걸쳐 계산된다. Inbound는 §7.3대로 On-board 이후 물량만
  포함한다.
- `[가정]` **Outbound**은 소스를 특정 시스템에 고정하지 않는다 — 수요예측
  (Demand Forecast)이든 확정 출고계획(Outbound Plan)이든 평균 출고량
  추정치든 고객 시스템에서 "주어지는 입력값"으로 취급한다(→ §9의 책임 경계).
  화면에는 Projected Inventory 정확도가 이 입력값의 정확도에 달려있다는
  점을 명시한다. In과 낙관/비관 방향을 맞출지는 미정(→ §11).

Safety Stock을 기준으로 미래 재고 상태를 3단계로 구분한다:

```
Projected Inventory > Safety Stock        → Normal
0 < Projected Inventory ≤ Safety Stock    → Risk
Projected Inventory ≤ 0                   → Shortage
```

단순 Shortage뿐 아니라 실제 재고가 남아 있더라도 Safety Stock 이하로 내려가는
위험 구간을 함께 보여준다. 주요 결과값: Risk 진입 시점, Shortage 발생 시점,
부족 수량, Shortage에서 회복하는 시점, Normal로 복귀하는 시점.

### 9.5 화면 구성

MVP의 중심 화면은 **Item별 통합 재고 흐름(Inventory Trend)**이다.
Shipment/Transportation 화면을 먼저 보여주는 것이 아니라 사용자가 최종적으로
알고 싶은 재고 결과(Normal → Risk → Shortage → Recovery로 변화하는 과정)를
먼저 보여준다.

**재고 투영(Projection)** `[설계]`
- Item별 일자별 Inventory Timeline: `Date | Inbound | Outbound | Projected`,
  Safety Stock 이하 및 음수 재고를 색으로 강조.
- Shortage는 별도 탭이 아니라 이 화면의 필터 뷰다 — Projected가 Safety
  Stock 이하 또는 음수로 내려가는 Item만 걸러서 보여준다.

**입고 예정(Inbound)** `[설계]`
- 표시 정보: Item, Qty, FDEST ETA, Delay Status.
- Delay Status 드릴다운 시 근거/신뢰도 제공(→ §7.5).

**신뢰도 노출 강화 — 설계 필요** `[구상]`
지금은 신뢰도가 "입고 예정" 리스트의 Delay Status 드릴다운 한 칸에만 있어,
§7의 핵심 산출물이 화면에서 잘 드러나지 않는다. 다음 요소는 방향만 있고
화면 사양은 아직 없다 — 별도 세션에서 설계 필요(진행 상황·우선순위는
`PROGRESS.md` "다음 단계" 참고):
- Inbound 건별 ETA 신뢰도의 1차 노출(드릴다운을 열기 전 단계에서 바로 보이는 지표)
- Init ETA 대비 갭 분포와 이번 건의 위치(→ §7.5 "당겨짐 케이스"에서 이미
  통계적으로 계산 가능함을 확인)
- 소스 간 상충 시 어느 값을 채택했는지 표시

**결과에서 원인으로 Drill-down** — 사용자는 복잡한 Transportation 정보를
먼저 이해할 필요 없이, 먼저 재고 결과를 보고 필요할 때만 내려간다:

```
Unified Inventory Trend → 특정 Risk/Shortage/변곡점 → 해당 일자의 Inbound/Outbound
→ Item Inbound → B/L/Container/Tracking → Initial FDEST ETA/현재 FDEST ETA
→ 관련 Vessel → Calling Port Schedule/PCP/CP → Port Congestion/관련 정보
```

**Item 상세** `[설계]` (재고 투영/입고 예정에서 드릴다운, 별도 탭 아님)
- Current Inventory, Projected Inventory Timeline, Inbound Schedule,
  Shipment History, B/L, Container Tracking. Transportation Tracking 클릭
  시 §8.1 화물 탭의 해당 컨테이너로 이동.
- Inbound 화물마다 근거/신뢰도 섹션 제공(→ §7.5).

**Overview KPI** `[구상]` (§6의 통합 Overview에 포함될 후보)
- Current Inventory, Inbound Inventory, Projected Inventory, Shortage Items,
  Today's Expected Receiving.
- ~~Items at Risk~~: Shortage Items와 구분이 불명확해 이번 버전에서는 제외.
  "아직 Shortage는 아니지만 Safety Stock에 근접" 같은 별도 정의가 필요하면
  추후 추가.

### 9.6 MVP에서 AI의 역할

이 절이 **AI 관여 범위 원칙의 정본**이다.

MVP의 핵심 계산(Inbound Qty 합산, Projected Inventory 계산, Safety Stock
비교, Normal/Risk/Shortage 판정) 자체를 AI가 담당할 필요는 없다 — 결정론적
계산으로 처리하는 것이 적합하다. AI는 Forwarding Intelligence를 발전시키는
과정에서 활용한다: Calling Port 상황 분석, Port Congestion 정보 해석,
Vessel/Port 관련 News 분석, ETA Risk Context 설명, 향후 ETA Confidence
고도화, 향후 Monitoring Agent.

> **AI가 Port Congestion이나 News를 보고 근거 없이 FDEST ETA를 임의
> 변경해서는 안 된다.** AI의 산출물은 설명·신뢰도 표시까지이며, 숫자 계산은
> 결정론적 로직이 담당한다.

### 9.7 MVP로 증명하려는 것

MVP의 목적은 완성된 시스템을 만드는 것이 아니라, **Forwarding의 Item-level
Inbound 정보가 재고 관리에 실제로 도움이 된다는 것을 증명**하는 것이다.
이 증명이 있어야 "도착 시점 정확도(§7)를 계속 끌어올릴 가치가 있다"는
것을 고객에게 설득력 있게 보여줄 수 있다 — 즉 MVP는 §7이 왜 중요한지를
뒷받침하는 근거를 만드는 단계다:

> W&D의 On-hand/Outbound/Safety Stock에 Forwarding의 Item-level In-transit
> Inventory를 결합하면, 고객이 미래의 재고 Risk와 Shortage를 더 일찍
> 이해하고 대응 판단을 내리는 데 실질적인 가치가 있다는 것을 End-to-End
> 사용자 경험으로 보여준다.

완벽한 자동화보다 이 가치를 눈으로 확인할 수 있는 End-to-End 경험을 먼저
만들고, 개발 과정에서 실제 데이터의 구조와 한계를 확인하면서 다음 버전을
구체화한다. 데이터 확보 방침은 → §9.2.

## 10. 참고 프로토타입 — vessel-tracker (실 AIS 연동)

- 위치: [`vessel-tracker/`](vessel-tracker/) — Node/Express + WebSocket 프록시 +
  Mapbox GL JS 지도. AIS 제공사 계정정보를 서버가 중계해 브라우저에 노출하지
  않는다.
- 데이터 소스: **SeaVantage(SVMP, `svmp.seavantage.com/api/v1`, Basic Auth)**.
  `GET /ship/search`로 MMSI→shipId를 조회해 캐싱하고, `GET
  /ship/snapshot/:shipId`를 주기적으로 폴링해 그 배 하나의 최신 위치를
  가져온다. 지역 제한 없이 전세계 조회가 가능하다.
- 화물 탭 목업의 선박 6척(ONE INNOVATION, MSC BRUNELLA, ONE COMMITMENT,
  HYUNDAI FAITH, CMA CGM AQUILA, MSC ISABELLA)은 MMSI 매핑 완료.
- **알려진 한계**:
  - REST 폴링 방식이라 실시간성이 폴링 주기(기본 15초)에 묶인다.
  - `/ship/snapshot/:shipId`의 `range` 파라미터 단위가 문서에 없어 분(minute)
    으로 가정하고 기본값 60을 쓴다(`SEAVANTAGE_SNAPSHOT_RANGE`로 조정 가능)
    — 실계정 테스트로 확인 필요.
  - **현재 SeaVantage API를 실제로 쓸 수 없다** — 계정이 whitelist에
    등록되지 않아 호출 자체가 막혀 있다. 등록 전까지는 실 데이터 연결
    검증이 불가능하다(whitelist 등록은 SeaVantage 측 처리 대기).
  - 이전 구현(aisstream.io)은 한국 해역 AIS 수신 커버리지가 없어 교체했다.
    그때 쓰던 "커버리지 있는 지역만 구독하는 데모 모드" 우회는 SeaVantage
    에서는 불필요해져 제거했다.
- 데모/검증 목적 프로토타입이며, 실서비스 적용 시 계약·SLA 조건은 별도 검토한다.

## 11. 미해결 이슈 / 의존성

본문에 `[가정]`/`[구상]`으로 이미 표시된 항목은 여기서 반복하지 않는다. 이
표는 **색인**이며 각 셀은 한 줄을 넘지 않는다 — 상세 서술은 본문 참조 섹션에
있다.

유형: `데이터 확보`(외부·타부서 의존) / `설계 미정`(우리가 정하면 되는 것) /
`범위 제외`(Phase 2 이관).

`데이터 확보` 행은 곧 해당 본문의 `[가정]` 태그가 `[검증]`/`[설계]`로
승격될 수 있는 조건이기도 하다 — 새 데이터·시스템 연동이 확보될 때마다
이 표를 먼저 점검해 승격 가능한 항목이 있는지 확인한다.

| 영역 | 이슈 | 유형 | 본문 참조 |
|---|---|---|---|
| 화물 탭 | 소스별 과거 예측 정확도 이력(PTA·정적 신뢰도의 전제) | 데이터 확보 | §7.1/§7.2 |
| 화물 탭 | 그래프DB(선박 스케줄·트래킹·AIS·혼잡도) 연동 방식 | 데이터 확보 | §7.1 |
| 화물 탭 | "리스크" 상태 판정 기준 | 설계 미정 | §7.2 |
| 화물 탭·Inventory | ETA/Inbound 신뢰도의 판단 근거 자체가 미정 — 갭 통계는 "반증 없음"만 말해줄 뿐 정확성을 확증하지 않음, 최소 표본 수 기준도 없음 | 설계 미정 | §7.4/§7.5 |
| 업무 탭 | Stuffing List 등 적입/장입 확정 데이터 — 대부분 Pantos 내부 시스템 연동 문제 | 데이터 확보 | §8.2 |
| 업무 탭 | Inland Routing 트럭/철도 스케줄·비용 마스터 데이터 | 데이터 확보 | §8.2 |
| 업무 탭 | 터미널 반출 제약(샤시 부족·운송사 Capa) 데이터 소스 미정 — 이 데이터를 관리하는 포트·터미널에서만 조건부 노출 | 데이터 확보 | §8.2 |
| 업무 탭 | Appointment(화주 인도 약속일) 확정 로직·연동 시스템 미정 | 설계 미정 | §8.2 |
| 업무·Inventory | Pantos 실제 CI/PL 입수 경로(구조화 파일 vs 이미지/PDF) 미확인 | 데이터 확보 | §8.5 |
| 비용 탭 | 대안 루트 비교 데이터(대안 스케줄·실시간 선복·운임·탄소계수) | 데이터 확보 | §8.4 |
| Inventory | On-hand(W&D) 재고 연동 방식 | 데이터 확보 | §9.2 |
| Inventory | Outbound 소스와 그 정확도(수요예측/확정 출고계획/평균값 등) | 데이터 확보 | §9.4 |
| Inventory | Safety Stock 마스터 데이터 등록 여부 | 데이터 확보 | §9.2 |
| Inventory | Vessel·Port News 데이터의 실제 연결 방식 | 데이터 확보 | §9.2 |
| Inventory | 입고 버퍼 일수 — 완료건(FDEST ATA) 0건이라 실측 불가 | 데이터 확보 | §7.3 |
| Inventory | Inbound 인식에 PTA P95 반영 — PTA 모델 확보 전제, MVP는 단일값 대체 | 데이터 확보 | §7.3 |
| Inventory | `Incident List.xlsx` — 커버 항로가 현 데이터셋과 불일치해 미사용 | 데이터 확보 | §7.5 |
| Inventory | WebSearch 신선도 임계값(2주 기준의 기사 유형별 차등) | 설계 미정 | §7.5 |
| Inventory | On-board 상태를 어떤 필드로 판정할지(현재 POL ATD 존재 여부) | 설계 미정 | §7.3 |
| Inventory | In과 Out의 낙관/비관 방향을 맞출지 | 설계 미정 | §9.4 |
| Inventory | Port Congestion/Calling Schedule ↔ ETA 변화의 정량적 인과모델 | 설계 미정 | §9.2 |
| Inventory | 재고관리 Agent의 필요 범위 — 임계값 판단은 룰 베이스로 충분, 종합 판단 영역만 Agent | 설계 미정 | §9 |
| Inventory | POD 양하 시 Shortage 임박도 기반 우선순위 배정 로직 | 범위 제외 | §9 |
| Inventory | 선사 ETA 자체 보정 방법론 | 범위 제외 | §7.4 |
| Overview | 구성 방식(설계자 우선순위 선정 vs 사용자 커스터마이즈) | 설계 미정 | §6 |

## 12. 범위 제외 (Out of Scope)

- 수배송·창고 관리, WMS 운영 기능(Receiving/Picking/Putaway/Cycle Count)
- 철도/트럭 본선 운송(내륙운송/Inland Routing 제외)
- 고객 세그먼트별 화면 분기, 역할별(마케팅/물류담당 등) 화면 재구성
- 액션 처리시간(SLA) 측정
- 수요예측 모델 자체의 생성(플랫폼이 수요를 만들어내지 않음 — → §9.2),
  출고계획 수립, 재고 최적화 엔진
- AI Q&A(질의 범위·응답 데이터·다른 AI 기능과의 관계가 정의되지 않아 §6 IA에서
  제외). 필요성·범위를 다시 정리하면 재검토할 수 있다.
- 발주 실행 자체(PO 생성·공급처 전달·Booking) — Shortage 발생 시 기존
  Supply Pipeline과 신규 조달 가능성을 검토해 발주를 권고(Recommendation)
  하는 것까지가 장기 방향이며, 실제 발주를 생성·전달·확정하는 실행 단계는
  범위 밖이다(→ §9).

진행 현황(탭별 상태)과 다음 단계(할 일 목록)는 이 문서가 아니라
[`PROGRESS.md`](PROGRESS.md) "다음 단계"가 정본이다 — 둘 다 자주 바뀌는
현재 상태 정보라 PRD(사양 문서)보다 그쪽 성격에 맞고, PRD에 별도로 두면
같은 정보가 두 곳에서 따로 갱신되며 어긋날 위험이 있다.
