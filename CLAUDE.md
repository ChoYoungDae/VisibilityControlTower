# Pantos View — Visibility Control Tower

세션 시작 시 항상 [`PROGRESS.md`](PROGRESS.md)를 먼저 읽고, 거기 적힌 "다음 단계"부터 이어간다.
콘텐츠의 상세 근거가 필요하면 [`PantosView_VisibilityControlTower_기획.md`](PantosView_VisibilityControlTower_기획.md)를 참고한다.

작업을 진행하다 방향이나 다음 할 일이 바뀌면, Claude가 세션을 끝내기 전에
PROGRESS.md의 "다음 단계" 섹션을 그 시점 기준으로 직접 갱신한다 (사용자가 수동으로
고칠 필요 없음) — 이 섹션은 자동으로 최신 상태가 유지되지 않으므로 세션마다 챙겨야 한다.

세션 시작 시 "다음 단계"가 비어있거나, 불명확하거나, 앞선 대화 흐름과 안 맞아
보이면 — 임의로 다음 할 일을 추측해서 진행하지 말고, 사용자에게 무엇부터 할지
물어본다.

## Git 커밋 규칙

사용자가 명시적으로 커밋을 요청하거나, 대화 세션을 마무리하는 시점이 아니면
커밋하지 않는다. 파일을 만들거나 고칠 때마다 커밋하지 않는다.

## 화면 텍스트 언어 규칙

화면(목업/실제 구현 모두)에 들어가는 텍스트는:
- **물류 업계 용어는 영어로** 쓴다 (예: Pre-carriage / Main-carriage / On-carriage, ETA, PTA, TS, D&D, LFD, MMSI 등 — 업계에서 관용적으로 영어를 쓰는 용어들).
- **그 외 일반적인 설명·안내 문구는 한글로** 쓴다 (예: 버튼 설명, 안내 노트, 툴팁, 에러 메시지 등 업계 전문용어가 아닌 것).
- 상단 탭바(Overview/화물/업무/비용)처럼 이미 별도로 확정된 한글 명칭은 이 규칙과 무관하게 유지한다.
- 이 규칙은 화물 탭뿐 아니라 프로젝트 전체(업무 탭, 비용 탭, Overview 등)에 공통 적용한다.
