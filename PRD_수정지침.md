# VisibilityControlTower_PRD.md 수정 지침

- 대상 문서: `VisibilityControlTower_PRD.md` (v1.4, 771줄)
- 목적: (1) 중복 제거, (2) 논지 재배치 — "Inbound 시점 신뢰도"를 문서의 중심축으로, (3) 검증된 사실과 가정의 분리
- 실행 주체: Claude Code
- **중요**: 아래 1단계를 먼저 수행하고 사용자 리뷰를 받은 뒤 2단계로 진행할 것. 두 단계를 한 번에 커밋하지 말 것.

---

## 0. 작업 전 원칙

### 0.1 내용을 삭제하지 말 것
중복 제거는 **같은 내용이 여러 곳에 서술된 것을 한 곳으로 모으는 작업**이다. 정보 자체를 버리는 작업이 아니다. 어떤 판단·근거·수치도 문서에서 사라지면 안 된다. 옮기고, 합치고, 참조로 대체하는 것만 허용한다.

### 0.2 결정 이력은 본문에서 분리
현재 본문에 `2026-08-05 세션에서 전환 확정`, `(2026-08-08 프로토타입에서 신규 확인)`, `(사용자 명시적 정정)` 같은 이력 표기가 산재해 있다. 이것이 중복의 최대 원인이다 — 새 결정이 날 때마다 기존 문단을 고치지 않고 새 문단을 덧붙이면서 "정정", "재정의" 딱지를 붙였기 때문이다.

**규칙**: 본문은 **현재 사양만** 서술한다. 언제 왜 바뀌었는지는 이미 존재하는 `PROGRESS.md`가 담당한다.
- 이력 표기는 원칙적으로 제거한다.
- 예외: 그 결정의 *근거*가 현재 사양을 이해하는 데 필요한 경우(예: §5의 PO→CI/PL 전환 사유)에는 근거만 남기고 날짜·세션 표기는 뺀다.
- 제거하는 이력 중 `PROGRESS.md`에 없는 내용이 있으면 `PROGRESS.md`에 먼저 추가한 뒤 본문에서 뺀다.

### 0.3 단일 출처(Single Source of Truth) 지정
각 개념마다 **정본 위치를 한 곳만** 정하고, 다른 곳에서는 한 줄 참조로 대체한다. 참조는 `(→ §X.X)` 형식으로 통일한다.

| 개념 | 정본 | 나머지 위치에서의 처리 |
|---|---|---|
| PO를 추적하지 않는 이유 / CI-PL이 Item 출처인 이유 | §5 | §7.2, §8.1, §10에서 서술 삭제 → `(→ §5)` |
| CI/PL에서 Item·QTY를 추출하는 방식 | §7.5 | §8.1, §8.2, §10에서 서술 삭제 → `(→ §7.5)` |
| FDEST ETA 산출·소스 상충 판정·신뢰도 | §7.1 (2단계 후 신설 §7) | §8.4, §8.6, §8.6.1의 중복 서술 삭제 |
| Inbound 인식 시점 규칙 | §8.4 | §7.1, §7.2, §8.2 표에서 서술 삭제 → `(→ §8.4)` |
| AI가 ETA 숫자를 임의 변경하지 않는다는 원칙 | §8.8 | §7.5, §8.6.1에서 반복 삭제 → `(→ §8.8)` |
| raw data 샘플의 한계(Model/QTY가 예외적으로 구조화돼 있음) | §8.2 | §8.1, §10에서 삭제 |
| 합성데이터 사용 방침 | §8.2 | §8.9, §13에서 반복 삭제 |

---

## 1단계 — 무손실 중복 정리 (구조 변경 없음)

### 1.1 성숙도 태그 도입 (최우선 작업)

모든 기능 요구사항 항목 앞에 다음 4종 중 하나를 붙인다. 이것이 이번 수정에서 가장 중요한 변경이다.

| 태그 | 의미 | 판별 기준 |
|---|---|---|
| `[검증]` | 실데이터로 확인함 | 구체적 수치·건수가 문서에 있음 |
| `[설계]` | 사양 확정, 필요 데이터 확보됨, 미구현 | 데이터 소스가 실재하고 접근 가능함이 확인됨 |
| `[가정]` | 사양은 있으나 데이터 확보 여부 미확인 | "데이터가 있다는 전제로", "이런 데이터가 있다면" |
| `[구상]` | 방향성만 있고 사양 없음 | 화면·계산식·데이터 어느 것도 정의 안 됨 |

**적용 예시**(문서에서 그대로 판별 가능한 것들):
- `[검증]` §10 지연사유분석 조인 결과 — Tracking 155건 × CP_Vessel_List 조인 126건(81%) 매칭
- `[검증]` §8.6.1 당겨짐 케이스 — 155건 중 87건(56%)
- `[검증]` §8.4 W/H In Date 필드 실재 확인 / 단, 완료건 0건이라 버퍼 일수는 `[가정]`
- `[설계]` §8.4 Inbound 인식 시점(현재 FDEST ETA 단일값)
- `[설계]` §7.5 CI/PL Intake 경로 A/B / 단, 실제 입수 경로는 `[가정]`
- `[가정]` §7.2 Stuffing List 연동, Inland Routing 마스터 데이터
- `[가정]` §8.2 W&D On-hand·Outbound·Safety Stock 전부
- `[가정]` §8.2 News 데이터, 그래프DB 연동
- `[구상]` §7.3 비용 탭(화면 설계 착수 전), §7.4 대안 루트 추천, §8.10 Advanced 이후

**태그 도입의 부수 효과**: §10 미해결 이슈 표의 상당수 행이 "이건 아직 미정"을 말하려고 존재한다. 태그가 본문에 붙으면 그 행들은 불필요해진다 — §10은 **외부 의존성(우리가 못 푸는 것)만** 남기고 나머지는 삭제한다. 자세한 규칙은 1.3 참조.

### 1.2 중복 목록 — 개별 처리 지시

아래는 실제 확인된 중복이다. 줄 번호는 v1.4 기준이며 수정 과정에서 이동한다.

**(A) PO 배제 근거 — 3회**
- 위치: §5(L63-74), §7.2(L208-223), §8.1(L368-370)
- 처리: §5를 정본으로 유지. §7.2는 "최상위이자 유일한 리스트는 부킹 리스트다(PO를 별도 엔티티로 추적하지 않는 이유는 → §5)"로 축약. §8.1의 해당 불릿은 "PO No는 CI/PL에 찍힌 참조 라벨로만 노출(→ §5)"만 남김.

**(B) raw data 샘플의 한계 — 3회, 거의 동일 문장**
- 위치: §8.1(L371-374), §8.2(L380-386 괄호 안 긴 주의문), §10 CI/PL 행(L690)
- 처리: §8.2를 정본으로. §8.1은 "이 고객 샘플에서는 Item이 `Model` 컬럼, 수량이 `QTY`다(한계는 → §8.2)" 한 줄. §10 해당 행은 "Pantos 실제 CI/PL 입수 경로 미확인" 한 줄로 축약하고 나머지 서술 삭제.

**(C) FDEST ETA가 Inventory로 흘러간다 — 5회**
- 위치: §7.1 ETA/PTA 말미(L169-170), §7.1 지연사유분석 목적정정(L174-179), §7.2 도착준비 역방향 연동(L252-253), §8.2 표 Inbound 행(L397), §8.4
- 처리: §8.4를 정본으로. 나머지 4곳은 `FDEST ETA는 Inventory Inbound 인식 시점의 입력값이다(→ §8.4)` 한 줄로 통일. 같은 말을 매번 다르게 풀어쓰지 말 것.

**(D) PTA P95 채택 + 조기도착 리스크 — 3회**
- 위치: §7.1 ETA/PTA, §8.4 Target/현재MVP/리스크 3개 불릿(L440-456), §10 P95 행(L701)
- 처리: §8.4를 정본으로. §10 행은 `Inbound 인식에 P95 반영 — PTA 모델 확보 전제, MVP는 단일값 대체 (→ §8.4)`로 축약, 리스크 설명 문장 전부 삭제(§8.4에 이미 있음).

**(E) 지연사유분석 2단 구조(정적 신뢰도 + 건별 보정) — 4회**
- 위치: §7.1(L180-187), §8.6.1(L537), §10 PTA 행(L687), §14 1번
- 처리: §7.1을 정본으로. §8.6.1은 이미 "재사용한다"고 쓰고 있으므로 그 뒤의 재설명을 삭제.

**(F) 3-소스 조합 + 2주 신선도 기준 — 3회, 거의 전문 반복**
- 위치: §8.6.1 소스 불릿(L557-560), §10 지연사유분석 행(L688, 표 셀 하나가 문단 길이), §14 1번(L744-755)
- 처리: **§8.6.1을 정본**으로 하고 서술을 이쪽에 통합. §10 행은 `[검증]` 태그 + 결과 수치 한 줄만(`Tracking 155건 × CP_Vessel_List 조인 81% 매칭, Port Congestion은 POD 2곳 커버, Incident List는 항로 불일치로 미사용`). §14 1번은 **작업 지시만** 남기고 배경 설명 전부 삭제.

**(G) AI가 ETA를 임의 변경하지 않는다 — 3회**
- 위치: §7.5 공통원칙(L321-324), §8.6.1 원칙 불릿(L566-568), §8.8(L632-633)
- 처리: §8.8을 정본. 나머지는 `(→ §8.8)`.

**(H) 전체 Inbound 적용 + Shortage/Risk는 강조만 — 문서 내 최다 반복(6회 이상)**
- 위치: §8.6.1 안에서만 3회(스코프 문단 L538-544, 적용위치 L546-548, 구현순서 메모 L579-582), §8.7 입고예정(L599-600), §8.7 Item상세(L615-616), §14 1번
- 처리: §8.6.1 도입부에 **한 번만** 서술. §8.6.1 내부의 "구현순서 메모" 불릿은 삭제(스코프 문단과 같은 말). §8.7의 두 곳은 `근거/신뢰도 드릴다운 제공(→ §8.6.1)`으로 축약.

**(I) 합성데이터 방침 — 3회**
- 위치: §8.2(L401-404), §8.9(L644-647), §13
- 처리: §8.2 정본. §8.9는 가설 문장만 남김. §13은 산출물 컬럼에 `synthetic-data/`만.

### 1.3 §10 미해결 이슈 표 — 재정의

현재 §10은 **본문보다 상세한 서술**을 담고 있어 중복의 절반을 차지한다. 표는 색인이지 본문이 아니다.

**새 규칙**:
- 컬럼: `영역 | 이슈(한 줄) | 유형 | 본문 참조`
- `유형`은 다음 중 하나만: `데이터 확보`(외부/타부서 의존) / `설계 미정`(우리가 정하면 되는 것) / `범위 제외`(Phase 2 이관)
- **각 셀은 한 줄을 넘지 않는다.** 서술이 필요하면 본문 해당 섹션으로 옮긴다.
- 성숙도 태그(1.1)로 본문에 이미 `[가정]`/`[구상]`이 표시된 항목은 §10에서 **삭제**한다. 단 `유형=데이터 확보`인 항목은 남긴다(외부 의존성 목록으로서 가치가 있음).
- 삭제 대상 예: "Items at Risk 지표 정의"(§8.7에 이미 제외 명시), "Pre-carriage를 Planning Projection에 포함하는 규칙"(§8.4에 개념만 정의라고 이미 씀), "자체 ETA Prediction 모델"(§8.10에 이미 단계 표시), "AI Q&A"(§6에서 제외됐고 이력에 해당 → PROGRESS.md로)

### 1.4 §14 다음 단계 — 재정의

현재 §14의 각 항목이 §8.6.1·§7.5의 내용을 요약이 아니라 **재작성**하고 있다.

**새 규칙**: 각 항목은 `작업명 + 완료 기준 + 본문 참조` 3요소, 최대 2줄. 배경·근거·데이터 소스 설명은 전부 본문 참조로 대체.

예시(1번 항목):
```
1. [구현] Inbound 신뢰도 설명 Agent (→ §8.6.1)
   완료 기준: 입고 예정 리스트 전체 건에 근거 텍스트 생성, 매칭 실패 건 fallback 동작 확인
```

### 1.5 문체 통일
- 같은 대상을 `재고 탭` / `Inventory 탭` / `§8 Inventory`로 혼용 중 → §6에서 확정한 화면 명칭("재고", "재고 투영", "입고 예정")으로 통일하되, 도메인을 가리킬 때는 `Inventory 도메인`으로 구분.
- `Item`과 `Item/SKU` 혼용 → `Item`으로 통일(§8.1에서 SKU와 동일 단위임을 한 번만 명시).
- 강조(`**`)가 과다하다. 문단당 1회 이내로 줄인다. 지금은 강조가 너무 많아 강조 기능을 못 한다.

### 1.6 1단계 완료 기준
- 문서 길이가 **771줄 → 550줄 이하**로 감소했을 것(정보 손실 없이).
- 위 (A)~(I) 각 항목에 대해, 해당 내용이 문서에서 **정확히 한 곳에만** 서술되어 있을 것.
- 모든 기능 요구사항 항목에 성숙도 태그가 붙어 있을 것.
- 본문에 세션 날짜 표기가 남아 있지 않을 것(§ 헤더의 개정 이력 제외).
- **삭제된 내용이 없음을 확인**: 1단계 diff를 검토해 순수 삭제된 판단·근거·수치가 있으면 복원하거나 PROGRESS.md로 이동.

---

## 2단계 — 논지 재배치 (사용자 리뷰 후 진행)

### 2.1 배경 — 왜 재배치가 필요한가

이 문서의 논지는 §8 중간(L342-344)에 있다:

> 핵심은 Forwarding에서 Item별 Inbound 수량과 FDEST ETA를 최대한 정확하게 제공하는 것이다.

그런데 §1~§7(340줄)은 이 논지를 뒷받침하는 순서로 배열돼 있지 않고, 정확도 관련 내용(§7.1 ETA/PTA, §7.1 지연사유분석, §8.4 Inbound 인식, §8.6 ETA Confidence, §8.6.1 신뢰도 Agent)이 **두 개 챕터에 흩어져 서로를 계속 참조**한다. 1단계에서 확인한 중복 (C)(D)(E)(F)가 전부 여기서 발생한다. 즉 중복은 문체 문제가 아니라 **구조 문제**다.

### 2.2 §2 문제 정의 재작성 (가장 중요)

현재 §2 세 번째 불릿은 W&D의 한계를 **기능 부재**로 서술한다. 이를 **구조적 접근 불가**로 바꾼다.

- 현재: "W&D는 On-hand는 관리하지만 Inbound까지 결합한 미래 재고를 Item 단위로 보여주지 못한다"
- 변경 방향: 재고 계획의 정확도는 **입고 시점 정보의 정확도에 종속**되는데, 그 시점을 결정하는 근거(선사 milestone·AIS·내부 오퍼레이션 간 상충 판정)는 **포워더 안에만 존재**한다. 따라서 이 문제는 W&D/ERP 쪽에서 기능을 추가해서 풀리는 문제가 아니다.

이 한 문단이 바뀌면 나머지 700줄이 "그 시점을 어떻게 정확하게 만드는가"의 근거로 읽힌다.

### 2.3 §8 도입부에 책임 경계 명시

§8 목적 문단 바로 뒤에 다음 취지의 문단을 신설한다.

> Projected Inventory의 네 변수 중 On-hand·Outbound·Safety Stock은 고객 시스템에서 주어지는 입력값이며 본 서비스가 개선하지 않는다. 본 서비스가 정확도를 책임지는 변수는 **Inbound 시점 하나**다.

현재 이 내용은 §8.5 Outbound 불릿과 §11 범위 제외에 흩어져 있고, §10 미해결 이슈에는 같은 사실이 **약점으로** 다시 기록돼 있다. 한 곳에 경계로 선언하고 §10에서는 제거한다.

### 2.4 정확도 관련 내용을 한 장으로 통합

**신설**: `§7. 도착 시점 정확도 (Arrival Accuracy)` — 현재 §7 앞에 배치하거나 §7.1을 승격시켜 구성.

이 장으로 이동할 내용:
- 현 §7.1의 ETA/PTA 정의 (L156-170)
- 현 §7.1의 지연 사유 분석 = 소스 상충 판정 (L172-193)
- 현 §8.4의 Inbound 인식 시점 규칙 전체
- 현 §8.6 ETA Confidence
- 현 §8.6.1 Inbound 신뢰도 설명 Agent 전체

이동 후 각 원래 위치의 처리:
- §7.1 화물 탭 → **화면 사양만** 남김(리스트 구조, 상태 3단계 컬럼, 지연 표시, 선박 위치 연동). 방법론은 `(→ §7 정확도)`.
- §8.4 → `§7에서 산출한 도착 시점을 Item×Date로 합산해 Inbound로 인식한다` + Container-Item Mapping 유무별 계산 표만 남김.
- §8.6 / §8.6.1 → 섹션 자체 삭제, §8.7 화면 설명에서 `(→ §7)` 참조.

**기대 효과**: 중복 (C)(D)(E)(F) 4건이 구조적으로 소멸한다. 1단계에서 참조로만 정리했던 것이 실제로 한 곳에 모인다. 그리고 문서에 논지의 이름을 가진 장이 생긴다.

### 2.5 §8.7 화면 구성 보강

현재 재고 탭의 중심 화면은 Inventory Trend(Projection 타임라인, Normal/Risk/Shortage)이고, 신뢰도는 `Delay Status 컬럼의 드릴다운` 한 칸에 접혀 있다. 논지의 핵심 산출물이 드릴다운 안에 숨어 있는 구조다.

**추가 설계가 필요한 화면 요소**(사양 미정, 이번 수정에서는 요구사항으로만 기재):
- Inbound 건별 ETA 신뢰도의 **1차 노출**(드릴다운 이전 단계에서 보이는 지표)
- Init ETA 대비 갭 분포와 이번 건의 위치 — §8.6.1에서 이미 통계적으로 계산 가능하다고 확인된 값
- 소스 간 상충 시 어느 값을 채택했는지 표시

이 항목들은 `[구상]` 태그로 기재하고, 별도 세션에서 화면 설계할 것을 §14에 추가.

### 2.6 §8.10 로드맵 방향 재검토

현재 Target Vision의 화살표가 **발주 의사결정**을 향한다. §11에서 발주 실행을 제외했더라도, 로드맵이 가리키는 방향은 제품의 정체성 선언으로 읽힌다.

논지(정확도)와 정합적인 방향은 **정확도가 확장되는 축**이다 — 대상 항로 확대, 소스 추가, Confidence 모델 고도화, 그 정확도가 지원하는 의사결정의 확대. 발주 권고는 그 확장의 한 갈래로 배치한다. 현재는 발주 권고가 종착점으로 그려져 있다.

**주의**: 이건 사용자의 제품 전략 판단이 필요한 사안이다. Claude Code는 임의로 변경하지 말고 **변경 제안만 주석으로 남기고 사용자에게 확인**할 것.

### 2.7 2단계 완료 기준
- 정확도 관련 서술이 신설 §7 한 곳에 모여 있고, §8은 그 산출값의 **소비처**로 읽힐 것.
- §2를 읽은 사람이 "왜 이 문제를 포워더가 풀어야 하는가"에 답할 수 있을 것.
- §8 도입부에 책임 변수 경계가 명시돼 있고, §10에 같은 내용이 약점으로 중복 기재돼 있지 않을 것.
- 문서 길이 450줄 내외.

---

## 3. 하지 말아야 할 것

- **§10·§14를 없애지 말 것.** 축소가 목적이지 삭제가 아니다. 외부 의존성 목록과 작업 우선순위는 이 문서의 실용적 가치다.
- **`[가정]`·`[구상]` 항목을 삭제하지 말 것.** 미래 방향성을 설계하기 위해 의도적으로 넣은 내용이다. 태그로 구분만 하면 된다.
- **§12 벤치마크를 확장하지 말 것.** 현재 분량이 적절하다.
- **§9 vessel-tracker의 "알려진 한계" 서술을 축약하지 말 것.** 프로토타입의 실패 조건을 기록한 드문 부분이다.
- 새로운 요구사항을 추가하지 말 것. 이번 작업은 재배치·정리에 한정한다.
